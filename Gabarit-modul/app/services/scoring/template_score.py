from typing import Dict

def aggregate_scores(template_scores: Dict[str, float]) -> Dict[str, float]:
    """
    Placeholder for future complex scoring (weights, calibrations).
    For now return same scores.
    """
    return template_scores
